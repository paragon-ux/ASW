# Extension Fixtures

- `valid/` and `invalid/` exercise extension schemas.
- `scenarios/` contains the controlled scenario manifests the fresh agent should implement.

The target implementation should provide a small validation command or test that proves all valid fixtures pass and all invalid fixtures fail.
