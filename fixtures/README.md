# Fixtures

`valid/` contains schema-valid contract examples. `invalid/` contains JSON examples that MUST fail schema validation plus `semantic-cases.md` for cross-record invariants that require runtime tests. `evaluation/` contains comparative scenario/profile inputs.

Run:

```text
python validate_fixtures.py
```
